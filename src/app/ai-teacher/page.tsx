import MainLayout from '../(main)/layout';
import AITeacherPage from '../(main)/ai-teacher/page';

export default function Page() {
  return (
    <MainLayout>
      <AITeacherPage />
    </MainLayout>
  );
}
