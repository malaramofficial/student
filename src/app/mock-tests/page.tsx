import MainLayout from '../(main)/layout';
import MockTestsPage from '../(main)/mock-tests/page';

export default function Page() {
  return (
    <MainLayout>
      <MockTestsPage />
    </MainLayout>
  );
}
