import MainLayout from '../(main)/layout';
import PreviousPapersPage from '../(main)/previous-papers/page';

export default function Page() {
  return (
    <MainLayout>
      <PreviousPapersPage />
    </MainLayout>
  );
}
