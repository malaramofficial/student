import MainLayout from '../(main)/layout';
import ResultsPage from '../(main)/results/page';

export default function Page() {
  return (
    <MainLayout>
      <ResultsPage />
    </MainLayout>
  );
}
